sudo: false
language: python
matrix:
  include:
  - python: "3.5"
    dist: xenial
  - python: "3.6"
    dist: xenial
  - python: "3.7"
    dist: bionic
install:
  - pip install -q pytest flake8 pip>=10 tox-travis
  - pip install tox
  - sudo apt-get install snapcraft
script:
  - tox
  - tox -e lint
  - tox -e docs
  - snapcraft
deploy:
  provider: snap
  snap: jujuna_*_amd64.snap
  channel: edge
  skip_cleanup: true
  on:
    branch: master
    python: "3.6"
